<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">Beauty Cure</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <%= link_to 'Accueil', root_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%= link_to 'Produits', products_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                <%if current_user %>
                    <%= link_to "Panier (#{current_user.cart.total_item})", user_cart_path(current_user.id, current_user.cart.id), class: 'nav-link' %>
                <%end%>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Dropdown link
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if current_user %>
                <%= link_to 'Se déconnecter', destroy_user_session_path, method: 'delete', class: 'dropdown-item' %>
                <%= link_to 'Mon profil', user_user_information_path(current_user.id, current_user.user_information.id), class: 'dropdown-item' %>
            <%else%>
                <%= link_to 'Se connecter', new_user_session_path, class: 'dropdown-item' %>
                <%= link_to "S'enregistrer", new_user_registration_path, class: 'dropdown-item' %>
            <%end%>
            </div>
            </li>
        </ul>
    </div>
</nav>